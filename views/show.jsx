const React = require('react');
const Def = require('./default');

const Show = () => {
    return (
        <Def>
            <main>
                <h2>Show page</h2>
            </main>
        </Def>
    );
};

module.exports = Show;
