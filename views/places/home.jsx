const React = require('react');
const Def = require('../default');

const Home = () => {
    return (
        <Def>
            <main>
                <h1>Home page for places dir.</h1>
            </main>
        </Def>
    );
};

module.exports = Home;
