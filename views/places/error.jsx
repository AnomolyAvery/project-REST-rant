const React = require('react');
const Def = require('../default');

const Error = () => {
    return (
        <Def>
            <main>
                <h1>Error page for places dir.</h1>
            </main>
        </Def>
    );
};

module.exports = Error;
